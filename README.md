# Wildberries Catalog Parser

Скрипт для автоматизированного сбора данных о товарах с маркетплейса Wildberries. 
Разработан в рамках тестового задания.

## Функционал
- Обход защиты API (имитация TLS-отпечатка Chrome).
- Динамический расчет CDN-серверов (корзин) WB для сбора медиа-данных и описаний.
- Фильтрация данных по заданным параметрам (рейтинг, цена, страна производства).
- Экспорт результатов в форматы `.xlsx` (полный каталог и отфильтрованная выборка).

## Стек технологий
- **Python 3.10+**
- `curl_cffi` — для обхода защиты от ботов (429 Too Many Requests).
- `pandas` / `openpyxl` — для обработки данных и сохранения в Excel.

## Установка и запуск

1. Склонируйте репозиторий:
   ```bash
   git clone <ваша_ссылка_на_репозиторий>
   
2. Создайте и активируйте виртуальное окружение:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Для Linux/Mac
   venv\Scripts\activate     # Для Windows
   
3. Установите зависимости:
   ```bash
   pip install -r requirements.txt

4. Запустите скрипт:
   ```bash
   python parser.py

### Шаг 3. Фиксируем зависимости
Чтобы команда `pip install -r requirements.txt` из README сработала, этот файл нужно создать. Выполни в терминале:
```bash
pip freeze > requirements.txt